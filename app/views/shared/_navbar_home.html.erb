<nav class="navbar-lewagon navbar navbar-expand-sm navbar-light">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <!-- Logo à gauche -->
    <div>
      <%= link_to root_path, class: "navbar-brand" do %>
        <%= image_tag "logo-gardenbnb.png", alt: "GardenBnb", class: "navbar-logo" %>
      <% end %>
    </div>

    <!-- Boutons à droite -->
    <div class="d-flex align-items-center gap-3">
      <% if user_signed_in? %>
        <!-- Lien 'Louer votre jardin' visible si connecté -->
        <%= link_to "Louer votre jardin", new_garden_path, class: "btn btn-outline-success fw-bold" %>

        <!-- Avatar utilisateur -->
        <div class="nav-item dropdown">
          <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <% if current_user.avatar.attached? %>
              <%= image_tag current_user.avatar, class: "avatar", alt: "dropdown menu" %>
            <% else %>
              <%= image_tag "https://ui-avatars.com/api/?name=#{current_user.first_name}+#{current_user.last_name}&background=2d5a27&color=fff", class: "avatar", alt: "dropdown menu" %>
            <% end %>
          </a>
          <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
            <%= link_to "Mon profil", edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "Déconnexion", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" %>
          </div>
        </div>
      <% else %>
        <!-- Lien 'Louer votre jardin' masqué si pas connecté (facultatif, sinon tu peux le mettre ici aussi) -->
        <%= link_to "Connexion", new_user_session_path, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</nav>
