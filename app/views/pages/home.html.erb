<div class="banner">
  <div class="banner-content">
    <h1>Envie de verdure ? Découvrez des jardins privés exceptionnels.</h1>
    <p>La location d'espaces verts entre particuliers.</p>
    <%= link_to "Découvrir les jardins", gardens_path, class: "btn btn-primary btn-lg" %>
  </div>
</div>

<div class="container mt-5">
  <div class="row">
    <div class="col-12">
      <h2 class="text-center mb-4">Jardins en vedette</h2>
    </div>
  </div>
  <div class="row">
    <% @featured_gardens.each do |garden| %>
      <div class="col-md-4 mb-4">
        <div class="card">
          <% if garden.photos.attached? %>
            <%= cl_image_tag garden.photos.first.key,
                      width: 400, height: 200, crop: :fill,
                      class: "card-img-top",
                      alt: garden.title %>
          <% else %>
            <div class="bg-light d-flex align-items-center justify-content-center" style="height: 200px;">
              <div class="text-center text-muted">
                <i class="fas fa-seedling fa-2x mb-2"></i>
                <p class="mb-0 small">Aucune photo</p>
              </div>
            </div>
          <% end %>
          <div class="card-body">
            <h5 class="card-title"><%= garden.title %></h5>
            <p class="card-text"><%= garden.description %></p>
            <p class="text-success"><strong><%= garden.price_per_day %>€/jour</strong></p>
            <%= link_to "Voir détails", garden_path(garden), class: "btn btn-outline-primary" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
